#!/bin/bash
set -eu

mkdir -p ${BOSH_INSTALL_TARGET}/bin
tar -xf containerd/crictl-v1.17.0-linux-amd64.tar.gz   -C ${BOSH_INSTALL_TARGET}/bin
tar -xf containerd/cni-plugins-linux-amd64-v0.8.5.tgz  -C ${BOSH_INSTALL_TARGET}/bin
tar -xf containerd/containerd-1.3.2.linux-amd64.tar.gz -C ${BOSH_INSTALL_TARGET}/
